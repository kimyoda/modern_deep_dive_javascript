# 10.6 프로퍼티 값 갱신

---

이미 존재하는 프로퍼티에 값을 할당하면 프로퍼티 값이 갱신된다.
예제 10 - 16

```js
var person = {
  name: "Kim",
};

// person 객체에 name 프로퍼티가 존재하여 name 프로퍼티의 값이 갱신된다.
person.name = "Kim";

cosnole.log(person); // {name: "Kim"}
```
